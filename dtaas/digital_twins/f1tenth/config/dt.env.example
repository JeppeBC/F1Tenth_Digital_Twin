# Copy this file to dt.env (same folder) and adjust for your machine.
# dt.env is machine-specific and gitignored — do not commit it.

# Absolute path to the workspace-dex-localhost folder that contains its
# docker-compose.yml (the base DTaaS stack started earlier).
DTAAS_DIR="C:/Users/bangs/Desktop/AU/workspace-dex-localhost/workspace-dex-localhost"

# IP address of the physical F1Tenth car (dt_pt_listener.py), reachable
# from this machine over WiFi/hotspot.
PT_HOST=172.20.10.8
